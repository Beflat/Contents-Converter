imports:
    - { resource: config.yml }

parameters:
    database_name:     cc_prod
    urbant_cconvert.content_dir_path: /var/www/data/cc.urban-theory.net/src/app/contents

#doctrine:
#    orm:
#        metadata_cache_driver: apc
#        result_cache_driver: apc
#        query_cache_driver: apc

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
        nested:
            type:  stream
            path:  %kernel.logs_dir%/%kernel.environment%.log
            level: debug
